import { Studio } from "@/components/Studio";

export const metadata = {
  title: "Studio — card-portfolio",
  description: "마크다운 이력서를 업로드하면 명함과 포트폴리오 페이지를 자동 생성합니다.",
};

export default function StudioPage() {
  return <Studio />;
}
